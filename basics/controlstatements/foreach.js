const days=['sunday', 'monday', 'tuesday', 'wednesday', 'thursday', 'friday', 'saturday'];
//days.forEach(function(day, index){
  //  console.log(`starts with ${day} ${index}`);
//});
for (let index = 0; index < days.length; index++) {
    const element = days[index];
    console.log(element);
}